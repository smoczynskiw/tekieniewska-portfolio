version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.16-browsers
    steps:
      - run: sudo yarn global add @lhci/cli
      - run: lhci autorun
      - store_artifacts:
          path: ~/tekieniewska-portfolio/lhci-report
